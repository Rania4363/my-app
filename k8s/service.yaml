apiVersion: v1
kind: Service
metadata:
  name: springboot-app
  namespace: default
spec:
  selector:
    app: springboot-app
  type: NodePort
  ports:
    - protocol: TCP
      port: 80
      targetPort: 8080
      nodePort: 30080
